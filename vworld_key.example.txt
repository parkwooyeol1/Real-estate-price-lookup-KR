# VWorld 인증키 갱신용 파일 (예시)
#
# 사용법:
#   1) 이 파일을 같은 폴더에 'vworld_key.txt' 라는 이름으로 복사하세요.
#   2) 아래 example 값을 새로 발급받은 키로 바꾸세요.
#        - 1번째 줄(#으로 시작 안 하는 첫 줄): VWorld 인증키
#        - 2번째 줄(선택): 키 발급 시 등록한 도메인 (생략하면 localhost)
#   3) 프로그램을 다시 실행하면 이 키를 우선 사용합니다. (exe 재빌드 불필요)
#
# '#' 으로 시작하는 줄은 주석이라 무시됩니다.
#
# ↓↓↓ 아래 두 줄을 실제 값으로 바꾸세요 (vworld_key.txt 로 저장) ↓↓↓
여기에_발급받은_VWorld_인증키_입력
localhost
